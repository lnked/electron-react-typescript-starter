import * as React from 'react';
import { hot } from 'react-hot-loader/root';

const App = () => <div style={{ borderWidth: 1, borderStyle: 'solid', borderColor: 'red' }}>React App!</div>;

export default hot(App);